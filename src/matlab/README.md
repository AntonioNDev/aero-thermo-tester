# src/matlab

MATLAB-side code goes here. Scope this narrowly — see
`docs/architecture.md` for the recommended role (parallel-computing
benchmark is the suggested starting choice, with a structural check or
curve-fitting piece as alternatives).

Exchange data with Python via `.mat` files (Option 1 in `architecture.md`)
unless you specifically want to demonstrate the live MATLAB Engine API for
Python (Option 2).
