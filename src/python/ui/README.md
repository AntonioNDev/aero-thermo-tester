# ui (Phase 8)

The Streamlit app: object picker (built-in primitives + "upload your own
mesh"), wind speed slider, wind direction control, run button, and the
rendered output (3D pressure/heat view, Cd/Cl/temp readout, weak-point list
with suggestions).

Build this last. Prove the physics works in a plain script or notebook
first — debugging physics through a slow UI layer is a common way this kind
of project stalls (see TODO.md's "Common ways this stalls" section).
