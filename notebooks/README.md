# notebooks

Scratch space for prototyping the formulas in `docs/formulas.md` (Phase 3)
before wiring them into `src/python/`. Good for plotting Cp over a test
sphere, sanity-checking Reynolds numbers by hand, etc.

Keep final, reusable logic in `src/python/`, not here — notebooks are for
exploration, the package is what your tests and UI import from.
